org.gradle.jvmargs=-Xmx4096M --add-opens java.base/java.util=ALL-UNNAMED --add-opens java.base/java.lang.reflect=ALL-UNNAMED
org.gradle.daemon=true
org.gradle.parallel=true

minecraft_version=1.21.1
archives_base_name=mandrake
mod_version=1.0.1
maven_group=at.mandrake

# Dependencies
fabric_loader_version=0.16.10
fabric_api_version=0.106.0+1.21.1

neoforge_version=21.1.234

# Fabric Loom version
loom_version=1.10-SNAPSHOT

# NeoGradle version
neogradle_version=7.1.38

kotlin_version=2.0.21
kotlin.build.report.enabled=false

# Skip Vineflower decompiler (use binary patch mode instead - faster, less memory)
neogradle.subsystems.decompiler.enabled=false

org.gradle.logging.level=LIFECYCLE